# protobuf

前后端共享 protobuf 协议源文件目录。

第一阶段只定义最小链路协议：

- 登录与会话。
- 进入区域。
- KCP 输入帧。
- 区域快照。
- 技能事件。
- 伤害事件。
- 单人秘境开始与结算。

协议原则：

- 客户端上报意图，不上报最终结果。
- 服务端计算权威状态。
- 高频消息避免携带背包、任务等低频数据。
- 字段号发布后不复用。
- 删除字段时使用 `reserved`。
