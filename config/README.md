# config

配置表源文件与导出工具目录。

目录规划：

```text
config/
├─ source/   # CSV/XLSX 源表
├─ schema/   # 字段说明和校验规则
├─ export/   # 导出后的服务端/客户端配置
└─ tools/    # 导出、校验和差异检查工具
```

第一阶段只提供最小 CSV 示例表，用于支撑登录出生点、地图、区域、技能、怪物、单人秘境和奖励池。

配置原则：

- 主键必须唯一。
- 外键引用必须可校验。
- 必填字段不得为空。
- 不使用默认假数据掩盖配置错误。
